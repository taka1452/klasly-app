import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Your Studio - Klasly",
};

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
